<table>
    <th>Date</th>
    <th>Time</th>
    <th>Horse</th>
    <th>Rider</th>
    <th>Notes</th>
    <th>Update</th>
    <tr>
        <td><%= @ride.format_date %></td>
        <td><%= @ride.format_time %></td>
        <td><%= link_to @ride.horse.name, horse_path(@ride.horse) %></td>
        <td><%= link_to @ride.rider.name, rider_path(@rider) %></td>
        <td><%= @ride.notes %></td>
    </tr>
</table>


