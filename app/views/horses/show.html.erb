<h2><%= @horse.name %></h2>
<h4>Owner: <%= @horse.owner %></h4>

<td><%= link_to "Edit Horse Info", edit_horse_path(@horse) %>
<td><%= button_to "Remove Horse", horse_path(@horse), method: :delete %></td>

<h3>All Rides</h3>

<table>
    <th>Date</th>
    <th>Time</th>
    <th>Rider</th>
    <th>Notes</th>
    <% sort_horse_rides(@horse).each do |ride| %>
    <tr>
        <td><%= ride.format_date %></td>
        <td><%= ride.format_time %></td>
        <td><%= link_to ride.rider.name, rider_path(ride.rider) %></td>
        <td><%= ride.notes %></td>
    </tr>
    <% end %>
</table>